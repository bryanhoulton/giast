{
  "buildCommand": "yarn build:packages && cd demo && yarn build",
  "outputDirectory": "demo/dist",
  "installCommand": "yarn install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
